diff --git a/gen_and_telmetry.py b/gen_and_telmetry.py
index 5ad9ec8..b72f4d2 100644
--- a/gen_and_telmetry.py
+++ b/gen_and_telmetry.py
@@ -14,6 +14,7 @@ from pathlib import Path
 from typing import Callable, Dict, List, Any
 import inspect
 import itertools
+from scenario_identity import attach_scenario_identity, unique_scenarios
 import types
 import subprocess
 import os
@@ -1159,7 +1160,7 @@ def generate_multifault_specs(single_specs, max_pairs=500):
             f"__{a.get('task')}"
         )
 
-        yield {
+        yield attach_scenario_identity({
             "problem_id": problem_id,
             "fault_family": "multifault",
             "task": a.get("task"),
@@ -1167,7 +1168,7 @@ def generate_multifault_specs(single_specs, max_pairs=500):
             "deployment": "k8s",
             "is_multifault": True,
             "subproblems": [a, b],
-        }
+        })
 
         produced += 1
 
@@ -1967,7 +1968,8 @@ async def main():
     # ── Pre-warm: pull all images into the local cache once ──────────────────
     _prewarm_social_network()
 
-    all_specs = list(generate_specs())
+    all_specs = unique_scenarios([attach_scenario_identity(s) if not s.get("scenario_spec_sha256") else s
+                                  for s in generate_specs()])
 
     runnable_specs = []
     skipped = 0
