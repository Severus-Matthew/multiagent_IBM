# Copy values into a private, untracked environment file or export them in the
# launching shell. Never commit the real key.
OPENAI_API_KEY=replace-with-your-openai-api-key
HF_HOME=/mnt/aiops-training/cache/huggingface
